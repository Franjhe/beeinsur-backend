import express from 'express';
import authenticate from '../../middlewares/authenticate.js'
import galleyController from '../../controllers/newsController.js';

const router = express.Router();

router


export default router;